from tkinter import *

root = Tk() # creating a blank Window
first_label = Label(root, text="Welcome to Python Tkinter") # creating a label
first_label.pack() # pack everything inside the window and display
first_label.mainloop() # so that the output constantly displays until the user decides to close the window
