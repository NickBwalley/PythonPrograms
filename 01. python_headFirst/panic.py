phrase = "Don't panic!"
plist = list(phrase)
print(phrase)
print(plist)

new_phrase = ''.join(plist)
print(plist)
print(new_phrase)

# OPERATIONS TO PERFORM AND RESULT TO BE >> "on tap"
#for i in range(4):>>This code used to pop the last4 objects in that string
 #   plist.pop()
  #  print(plist)
  
#plist.pop(0) >>This code is used to pop the element in index [0]
#print(plist)

#plist.extend([plist.pop(), plist.pop()])  >> This code is a concatenation of two or more lists to one  
#print(plist)
